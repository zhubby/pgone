# Changelog

## Unreleased

- Remove the `apiserver` and `proxy` subcommands along with their retired workspace crates.
- Add a global `--log-level` option for the unified `pgone` CLI.
- Add `pgone` unified CLI entrypoint with default GUI launch behavior.
- Add the `mcp-server` subcommand.
